import { observer } from 'mobx-react-lite';
import React from 'react';
import { PlatformView } from 'shared/components';

export const TransferAssetPicker = observer(() => {
  return <PlatformView></PlatformView>;
});
